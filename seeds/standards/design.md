# Design Standards

> **Placeholder** — customize per project. Owned by the Designer (proposes) and Project Owner (approves).

## Requirements (active)
- Every UI-bearing user story maps to a flow in the design spec (plugin `templates/design-spec.md`); every screen documents its states (default, empty, loading, error, success).
- Reuse documented components and patterns before introducing new ones; new components are flagged in the spec.
- Accessibility expectations are stated per screen, not assumed.

## Design system
_TBD — component library, tokens, spacing/typography scales._

## Interaction patterns
_TBD — validation feedback, confirmation dialogs, loading and error conventions._

## Accessibility baseline
_TBD — target WCAG level, keyboard support, screen-reader requirements._

## Responsive targets
_TBD — supported breakpoints/form factors._
