# Project Status

Maintained by the **Orchestrator**. One row per feature. Update on every phase transition, approval, or blocker change.

| Feature | Phase | Current Role | Blockers | Approvals |
|---|---|---|---|---|

Phases: `Requirements → UX Design (UI only) → Technical Design → Implementation → Review → Done`

Work that predates this organization records `pre-adoption` in **Approvals**: upstream gates waived, downstream gates apply in full. Never back-fill its missing artifacts (plugin `workflow/adoption.md`).
